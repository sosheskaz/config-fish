if status is-interactive
    # Commands to run in interactive sessions can go here
    # conf.d/ files auto-load at startup
    # functions/ lazy-load on first use
end

# Is stuff getting added below by over-eager integrations that you don't want?
# chmod a-w ~/.config/fish/config.fish
# Revert with
# chmod u+w ~/.config/fish/config.fish
